/**
 * POSTCSS YAPILANDIRMASI
 * Yazdığımız Tailwind CSS sınıflarını (Örn: bg-stone-50) standart CSS koduna dönüştürür.
 */
export default {
  plugins: {
    // Tailwind motorunu çalıştırır.
    tailwindcss: {},

    // CSS kodunu tarayıcı uyumluluğu için optimize eder.
    autoprefixer: {},
  },
};
